name: Wake Up Render Server

on:
  schedule:
    - cron: '*/5 * * * *'
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to Render
        run: |
          curl -v -L https://anas-chat.onrender.com
